# Copy to .env.local and fill in.
#
# Must be the same LiveKit Cloud project the Python agent points at — they meet
# in the room dispatched by /api/token.

LIVEKIT_URL=wss://<your-project>.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
